<nav class="dd-subnav" aria-label="Administration">
  <%= link_to "Agency", administration_agency_path, aria: { current: ("page" if current == :agency) } %>
  <%= link_to "Offices", administration_offices_path, aria: { current: ("page" if current == :offices) } %>
  <%= link_to "Users", administration_agency_users_path, aria: { current: ("page" if current == :users) } %>
</nav>

<nav class="dd-subnav" aria-label="Departures">
  <%= link_to "Departures", departures_path, class: "dd-subnav-item", aria: { current: ("page" if current == :departures) } %>
  <%= link_to "Travel programs", travel_programs_path, class: "dd-subnav-item", aria: { current: ("page" if current == :programs) } %>
  <% if local_assigns[:departure] %>
    <%= link_to "Overview", departure_path(departure), class: "dd-subnav-item", aria: { current: ("page" if current == :overview) } %>
    <%= link_to "Supplier planning", departure_supplier_arrangements_path(departure), class: "dd-subnav-item", aria: { current: ("page" if current == :supplier_planning) } %>
  <% end %>
</nav>
